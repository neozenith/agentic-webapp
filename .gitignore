# Scratch space (see global tmp/ rule) — also holds tmp/archived/ (mv'd files).
tmp/
.DS_Store

# Local agent config (own repo).
.claude/

# Coverage + test artifacts (pytest-cov, vitest v8, emulator logs).
.coverage
coverage/
htmlcov/
.pytest_cache/
.mmdc_cache/
.vitest/
.playwright-cli/
