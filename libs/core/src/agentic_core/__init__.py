"""agentic-core — shared cloud primitives.

The storage + database abstractions (and their GCS/BigQuery + in-memory
implementations) and shared models, used by both the FastAPI backend and the ADK
agent sidecar so they speak to GCS/BigQuery through one library.
"""

__version__ = "0.1.0"
